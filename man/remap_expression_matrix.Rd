% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expression-remap.R
\name{remap_expression_matrix}
\alias{remap_expression_matrix}
\title{Remap Expression Matrix with Updated Annotations}
\usage{
remap_expression_matrix(
  expression_matrix,
  updated_annotations,
  qc_flags = NULL,
  method = "median",
  remove_ambiguous = TRUE,
  min_probes = 1,
  log_transform = FALSE
)
}
\arguments{
\item{expression_matrix}{Matrix with probes as rows, samples as columns}

\item{updated_annotations}{Data frame with probe-to-gene mappings}

\item{qc_flags}{QC results from flag_problematic_probes (optional)}

\item{method}{Aggregation method: "mean", "median", "max", "iqr" (default: "median")}

\item{remove_ambiguous}{Remove ambiguous/multi-mapping probes (default: TRUE)}

\item{min_probes}{Minimum probes required per gene (default: 1)}

\item{log_transform}{Apply log2 transformation before aggregation (default: FALSE)}
}
\value{
Matrix with genes as rows, samples as columns
}
\description{
Collapses probe-level expression data to gene-level using updated annotations.
Handles multiple probes per gene and provides various aggregation methods.
}
\examples{
\dontrun{
  # Basic remapping with median aggregation
  gene_expr <- remap_expression_matrix(
    expression_matrix,
    annotations
  )

  # Use mean and filter QC
  gene_expr <- remap_expression_matrix(
    expression_matrix,
    annotations,
    qc_flags = qc_results,
    method = "mean",
    remove_ambiguous = TRUE
  )
}
}
