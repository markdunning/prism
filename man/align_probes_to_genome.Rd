% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/probe-alignment.R
\name{align_probes_to_genome}
\alias{align_probes_to_genome}
\title{Align Probe Sequences to Reference Genome}
\usage{
align_probes_to_genome(
  probe_sequences,
  genome,
  max_mismatches = 2,
  min_length = 20,
  report_progress = TRUE
)
}
\arguments{
\item{probe_sequences}{DNAStringSet of probe sequences}

\item{genome}{BSgenome object or DNAStringSet of chromosomes}

\item{max_mismatches}{Maximum number of mismatches allowed (default: 2)}

\item{min_length}{Minimum probe length to align (default: 20)}

\item{report_progress}{Show progress messages (default: TRUE)}
}
\value{
GRanges object with probe alignments
}
\description{
Maps probe sequences to genome using exact and fuzzy matching. Identifies
probe locations on both forward and reverse strands.
}
\examples{
\dontrun{
  library(BSgenome.Hsapiens.UCSC.hg38)

  # Get probe sequences
  sequences <- get_geo_probe_sequences("GPL570")

  # Align to genome
  alignments <- align_probes_to_genome(
    sequences,
    BSgenome.Hsapiens.UCSC.hg38,
    max_mismatches = 1
  )
}
}
